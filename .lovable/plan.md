## Add one-off class session

Insert a single `class_sessions` row (no schedule changes, won't recur):

- Class type: **Signature Flow** (50-min, Reformer Studio, cap 8) — matching the existing 11:00 Signature Flow pattern
- Instructor: **Duha A**
- Date: **Wed 2026-05-27**
- Time: **11:00–11:50**
- Room: Reformer Studio, max_capacity: 8
- schedule_id: null (standalone one-off, so weekly generator won't touch it)

Nothing else changes — recurring `class_schedules` and other weeks are untouched.

**Quick check:** there are two similar class types — `Signature Flow` and `Signature Flow Pilates – All Levels`. I'll use **`Signature Flow`**. Reply with "use pilates one" before approving if you meant the other.