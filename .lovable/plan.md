

## Add Google Analytics Tag

Add the Google Analytics gtag.js snippet to `index.html` immediately after the `<head>` tag.

### File: `index.html`
- Insert the two `<script>` tags (gtag.js loader + config) right after the opening `<head>` element, before the existing `<meta charset>` tag.
- Tag ID: `G-QNSF188FQC`

Single file, two lines of script. No other changes.

